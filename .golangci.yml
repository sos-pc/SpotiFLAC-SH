version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck

  exclusions:
    paths:
      - frontend/node_modules

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
