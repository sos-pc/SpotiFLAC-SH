# Dependencies of the shim itself. The engine core is installed from PyPI by the
# Dockerfile, together with upstream's own requirements.txt for that exact tag —
# so nothing upstream needs is duplicated here.

fastapi
uvicorn[standard]
pydantic>=2

# `requests>=2.31.0` used to be listed here as a gap-filler: it was imported by
# SpotiFLAC/core/signed_session_desktop.py while appearing in neither the
# project's dependencies nor its requirements.txt, so the package failed to
# import (hit on the first container run, 2026-07-23). Upstream declares it in
# both as of 1.5.9, so the workaround is gone.
#
# Gaps of that shape are handled by the Dockerfile now: it installs the tag's
# requirements.txt straight from upstream rather than us tracking which
# dependency they forgot this time.
